import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mv-hofkirchen-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
